# Data Service 設定
# =====================

# 是否使用 API（true = 呼叫 API，false = 讀取 JSON 檔案）
# 預設為 false，使用現有的 JSON 檔案
USE_API=false

# API 基礎路徑（USE_API=true 時使用）
API_BASE_URL=/api

# API 請求逾時時間（毫秒）
API_TIMEOUT=5000
